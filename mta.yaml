_schema-version: "3.2"
ID: bpset
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: bpset-destination-content
  type: com.sap.application.content
  requires:
  - name: bpset-destination-service
    parameters:
      content-target: true
  - name: bpset-repo-host
    parameters:
      service-key:
        name: bpset-repo-host-key
  - name: bpset-uaa
    parameters:
      service-key:
        name: bpset-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: bpset_html_repo_host
          ServiceInstanceName: bpset-html5-service
          ServiceKeyName: bpset-repo-host-key
          sap.cloud.service: bpset
        - Authentication: OAuth2UserTokenExchange
          Name: bpset_uaa
          ServiceInstanceName: bpset-xsuaa-service
          ServiceKeyName: bpset-uaa-key
          sap.cloud.service: bpset
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: bpset-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: bpset-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - bpset.zip
      name: bpset
      target-path: resources/
- name: bpset
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: bpset-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: bpset-destination-service
    service-plan: lite
- name: bpset-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: bpset-xsuaa-service
    service-plan: application
- name: bpset-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: bpset-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
